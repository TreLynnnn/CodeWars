// Create a method to see whether the string is ALL CAPS.

function isUpperCase(str){
    // if (str === str.toUpperCase()) {
    //     return true;
    // } else {
    //     return false;
    // }

    return str === str.toUpperCase() ? true : false ;
}
console.log(isUpperCase('DOG'), true)
